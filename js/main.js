// Generated by CoffeeScript 1.7.1
(function() {
  var genPhrase, randChoice, setNewPhrase, setupWords, template;

  template = ['intro', 'you', 'adjective', 'compound_adjective', 'noun'];

  randChoice = function(array) {
    return array[Math.floor(Math.random() * array.length)];
  };

  setupWords = function() {
    var k, v, w, _ref;
    _ref = this.words;
    for (k in _ref) {
      v = _ref[k];
      this.words[k] = v.trim().split(/\n/);
    }
    return this.words.intro = (function() {
      var _i, _len, _ref1, _results;
      _ref1 = this.words.intro;
      _results = [];
      for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
        w = _ref1[_i];
        _results.push("" + w + ",");
      }
      return _results;
    }).call(this);
  };

  genPhrase = function() {
    return template.map(function(type) {
      return randChoice(this.words[type]);
    }).join(' ').toUpperCase();
  };

  setNewPhrase = function() {
    return $('#sentence').text(genPhrase());
  };

  $(function() {
    setupWords();
    setNewPhrase(words);
    return $('#new').click(function() {
      return setNewPhrase();
    });
  });

}).call(this);
